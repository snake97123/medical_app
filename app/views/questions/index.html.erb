<%= render "shared/header" %>
<body>
<div class="question-lists">質問一覧</div>
 <% @questions.each do |question|%>
  <li class="list">
   <% if user_signed_in? %>
     <%= link_to  "#{question.title}" %>
     <%=l question.created_at %>
   <% end %>
  <% end %>
</body>
<%= render "shared/footer"%>


